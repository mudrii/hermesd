# hermesd autonomous dev loop — state

Source of truth: PLAN.md. Sequencing: B → A1 → C1 → A4 → A2 → A3 → A5 → A6 → C3 → C2 → NF1 → NF2 → NF3 → NF4 → NF5.

## 2026-06-14 — Tick 8 — C3 curator (DATA LAYER, DONE)

**Item:** C3 — curator panel (HIGH/L), sub-step A = model+collector+safe_collect. Commit 8ddd220.
**Why:** ~/.hermes/logs/curator/<stamp>/run.json (7 live runs) never read. Validated live shape read-only: started_at, duration_seconds, model, provider, counts{before,after,delta,archived_this_run,added_this_run,pruned_this_run,consolidated_this_run,tool_calls_total}, llm_summary, llm_error.
**Change-set:** models.py `CuratorRun` + `DashboardState.curator`. collector.py `_collect_curator` (newest dir by sorted name → run.json, NULL-tolerant, counts with len() fallback) + `curator` safe_collect source + pass-through.
**Test delta:** +4 (newest-run read, absent empty, empty-dir empty, resilience cache-preservation). 640→644.
**Gates:** 644 pass, ruff/format/mypy green. graphify updated.
**Next:** C3 sub-step B — `panels/curator_panel.py` (compact+detail, escape free-text llm_summary/error/model) + register PANEL_NAMES[13]/_RENDERERS + app.py _WIDE/_COMPACT/_TALL_NARROW layout + README/CLAUDE.md panel count + CHANGELOG + panel tests.

## 2026-06-14 — Tick 7 — FIX A6 (DONE)

**Item:** A6 — pr-monitor underscore/subdir families not globbed (MED/S). Commit 9660841.
**Why:** Live ~/.hermes has 7 pr-monitor files across 4 families (flat hyphen/underscore + pr-monitor/ + pr_monitor/ subdirs); hermesd globbed only flat hyphen → missed large active state (pr_monitor_state.json 61K etc). Validated live shapes read-only: ALL share repo/checked_at/prs/tracked_numbers/author_prs → parse-compatible.
**Change-set:** `collector.py` `_collect_pr_monitors` — glob 4 patterns, dedupe by repo keeping newest checked_at (repo-less keyed by filename), deterministic sort.
**Test delta:** +2 (underscore+subdir families read; same-repo dedupe newest wins). RED-first. 638→640.
**Decision:** chose dedupe-by-repo (canonical) over raw multi-row because ~5 of 7 live files are the same repo — would flood the panel.
**Gates:** 640 pass, ruff/format/mypy/pip-audit green. graphify updated. cache-preservation covered by existing operations corruption test.
**Next:** C3 (curator panel, L) — do ONE atomic sub-step per tick: model → collector+safe_collect → panel → __init__/layout reg → docs.

## 2026-06-14 — Tick 6 — FIX A3 + A5 (DONE)

**Item:** A3 pr-monitor keys (MED/S) + A5 dead checkedAt cleanup (LOW/S). Commit a2641fd.
**Why:** Live pr-monitor JSON keys prs/tracked_numbers/checked_at; collector counted retired monitored/tracked (→0) and tried dead camelCase checkedAt first.
**Change-set:** `collector.py` `_collect_pr_monitors` — monitored_count←len(prs) (legacy `monitored` fallback), tracked_count←len(tracked_numbers) (legacy `tracked` fallback), checked_at←`checked_at` only (dropped `checkedAt`).
**Test delta:** realistic-ified `sample_pr_monitor` fixture to live shape (RED: test_collector monitored_count 0!=2); +1 dedicated live-shape test. 637→638.
**Decision:** kept legacy monitored/tracked as `or` fallback (harmless resilience); dropped checkedAt entirely per A5.
**Gates:** 638 pass, ruff/mypy green. graphify updated.
**Next:** A6 (pr-monitor underscore/subdir families not globbed) — needs live-shape validation; then C3 (curator panel, L).

## 2026-06-14 — Tick 5 — FIX A2 (DONE)

**Item:** A2 — desktop-build-stamp camelCase (MED/S). Commit 63d07eb.
**Why:** Live desktop-build-stamp.json keys builtAt/contentHash/sourceMode; collector read version/stamp/built_at/created_at → always blank.
**Change-set:** `collector.py` `_collect_operations` — add `builtAt` to the `or` chain + fallback `contentHash[:12]`.
**Test delta:** +2 (builtAt populates; contentHash fallback). RED-first (blank). 635→637.
**Gates:** 637 pass, ruff/mypy green. graphify updated.
**Next:** A3 (pr-monitor tracked_numbers/prs keys).

## 2026-06-14 — Tick 4 — FIX A4 (DONE)

**Item:** A4 — cost prefix map included/exact→authoritative (MED/S). Commit 8e7b052.
**Why:** Live producer emits unknown/estimated/exact/included, never "reported"; authoritative `$` branch never taken → every cost showed `~$`.
**Change-set:** new shared `AUTHORITATIVE_COST_STATUSES = {reported,exact,included}` in models.py; `_session_cost_is_reported` (collector) + tokens.py prefix both use it. included(0.0) now authoritative $0.00 (no token estimate).
**Test delta:** +4 (resolved-cost included-zero; summarize all-included clears flag; corners included/exact; panel included/exact plain prefix). RED-first (got 0.325 / ~$). 630→635.
**Decision:** kept fn name `_session_cost_is_reported` (no rename churn); did NOT delete `reported` branch (test-load-bearing) per PLAN.
**Review:** self-review — no markup/IO/NULL concerns (pure status-set broaden). pip-audit clean.
**Gates:** 635 pass, ruff/mypy/pip-audit green. graphify updated.
**Next:** A2 (desktop-build-stamp camelCase: builtAt/contentHash/sourceMode).

## 2026-06-14 — Tick 3 — FIX C1 (DONE)

**Item:** C1 — gateway health enrichment (HIGH/S). Commit 234e265.
**Why:** `gateway_state.json` per-platform `error_code`/`error_message` + top-level `active_agents`/`restart_requested` never read.
**Change-set:** `models.py` PlatformStatus +error_code/error_message; GatewayState +active_agents/restart_requested. `collector.py` `_collect_gateway` populate (`or ""`, `_coerce_int`, `bool`). `panels/gateway.py` compact ⚠ marker on errored platform, detail Error column + restart/active-agent header lines.
**Test delta:** +3 tests (collector surfaces fields; panel detail shows error+restart+count; compact shows ⚠). RED-first (AttributeError, missing ⚠). 627→630.
**Review:** self-review — markup: detail escapes error_message, compact renders only ⚠ marker (no injection); NULL-tolerant; read-only; gateway already a safe_collect source (no new resilience test needed).
**Gates:** 630 pass, ruff/mypy/pip-audit green. graphify updated.
**Next:** A4 (cost prefix map included/exact→authoritative $).

## 2026-06-14 — Tick 2 — FIX A1 (DONE)

**Item:** FIX A1 — credential_pool list-vs-dict (HIGH/M). The flagship dead-panel bug.

**Why:** Live `auth.json` maps each provider → LIST of credential entries. hermesd fed the list to `_as_dict` → `{}` → every credential field blanked. Tests missed it (sample_auth still dict-of-dict).

**Change-set shipped (commit 1f5b360):**
- `collector.py` — new `_select_pool_entry(raw_entry)` helper (near `_as_dict`): list → pick min-priority entry (tie-break list order), filter empty/non-dict, empty→`{}`; dict shape passes through unchanged. `_collect_credential_pools` line 1181 now calls it instead of `_as_dict`.
- `_collect_providers` unchanged (outer is still a dict → `pool.keys()` = provider names; verified by reviewer).
- No new `CredentialPoolEntry` model fields (deferred per PLAN).

**Test delta:** +1 test `test_collect_credential_pool_accepts_list_shaped_entries` (inline-write, live dict-of-list shape, 2 entries → asserts min-priority wins + all fields populated). RED-first: failed `label == 'openai-codex'` (blanked to name). Suite 626→627.

**Decision:** pick **min-priority** entry as the provider representative (next credential to be used); ties keep list order. Inline test (not a conftest `sample_auth_list` fixture) — matches existing oauth-inference test pattern; both reviewers OK'd.

**Review:** 2 parallel adversarial reviewers → both CLEAN. Verified: empty-list/non-dict/missing-priority edge cases safe (min() guarded), secret redaction still works on picked entry, markup-escape intact in overview.py, cache-preservation holds (auth.json corruption test green), dict shape preserved.

**Gates:** pytest -W error::ResourceWarning (627 pass), ruff, ruff format, mypy, pip-audit — all green. graphify updated.

**Remaining / next:** FIX C1 (gateway health enrichment — error_message/error_code/active_agents/restart_requested, HIGH/S). Then A4 (cost prefix map), A2, A3, A5, A6.

## 2026-06-14 — Tick 1 — FIX B (DONE)

**Item:** FIX B — surface `end_reason` / `billing_base_url` / `billing_mode` (HIGH/S). First in sequence; unblocks C2 & NF1.

**Why:** Columns existed in state.db schema but hermesd never read them. Live data populated (end_reason cron_complete 1329 etc; 15 billing endpoints; billing_mode subscription_included 114).

**Change-set shipped (commit aea8b7f):**
- `db.py` wanted_columns — added 3 cols (PRAGMA-guarded allowlist).
- `models.py` SessionInfo — added `billing_base_url`/`billing_mode`/`end_reason` (str=""):
- `collector.py` `_collect_sessions` — mapped with `or ""` (NULL-tolerant).
- `panels/sessions.py` — surfaced 3 fields.

**Deviation (justified):** PLAN said add columns to the **main** sessions detail table. Implemented in the **Runtime sub-table** instead — 15 cols over-squeezed the main table at width 120 (`telegram` got ellipsized). Runtime sub-table is the established secondary per-session metadata surface; extended its filter to include the 3 new fields. Both review subagents judged the deviation sound.

**Test delta:** +0 files, edits to 3 tests. RED-first: `test_session_active.py::test_session_schema_fields_mapped` (mapping, +3 cols in INSERT+asserts), `test_panels_extended.py::test_sessions_panel_detail` (render +3 asserts incl full URL), `test_db_extended.py::test_read_sessions_excludes_private_payload_columns` (flipped the now-allowed `billing_base_url` assertion per PLAN gotcha). Suite 626 passed (was ~? prior).

**Review:** 2 parallel adversarial reviewers → CLEAN. 1 Minor (missing billing_base_url panel assertion) — fixed. 1 non-issue (sample_db fixture leaves new cols NULL) — dismissed: dedicated mapping test covers the path; PLAN says don't churn the dict fixtures.

**Gates:** pytest -W error::ResourceWarning (626 pass), ruff check, ruff format --check, mypy hermesd, pip-audit — all green. graphify updated.

**Remaining / next:** FIX A1 (credential_pool list-vs-dict, HIGH/M, the flagship dead-panel bug). Then C1 (gateway enrichment). C2 & NF1 now unblocked by B.
